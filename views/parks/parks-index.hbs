<div class="container coasters-index">
    <a href="/parks/new" class="btn btn-dark">Crear nuevo parque de atracciones</a>
    <h1 style="font-weight: 100">Índice de parque de atracciones</h1>
    {{#each Park}}

    <a href="/parks/{{_id}}" class="btn btn-sm btn-dark">Ver detalles</a>
    <h4>{{name}}</h4>
    <p>{{description}}</p>
    <hr>
    {{/each}}
</div>